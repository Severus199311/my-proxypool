倘若要添加一个爬取代理的爬虫，需要将新爬虫放在proxycrawler文件夹下，并在proxypool文件夹下的crawler模块中将其import并且添加到Crawler类中；
倘若要从外部导入一个代理，直接运行importer模块，
倘若要运行代理池，先配置settings模块，再运行run模块。
需要注意的是，flask的run方法是单线程的，因此在停止运行代理池后api接口也关闭了，意味着爬虫将无法获取代理。似乎只有将该代理池部署到某个外部服务器上才可以。